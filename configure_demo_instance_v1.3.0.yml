---
# -------------------------------------------------------
# Play 1 — Prepare execution environment on AAP runner
# Installs AWS CLI, SSM plugin, and community.aws collection
# on the AAP execution pod before Play 2 uses SSM connection
# -------------------------------------------------------
- name: Prepare execution environment
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
- name: Prepare execution environment
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Check Ansible version
      ansible.builtin.debug:
        msg: "{{ ansible_version.full }}"

    - name: Validate required variables are set
      ansible.builtin.assert:
        that:
          - ssm_instance_id is defined
          - ssm_instance_id != ''
          - ssm_instance_id != None
          - aws_region is defined
          - ssm_bucket_name is defined
        fail_msg: >
          Missing required variables. Ensure Terraform is passing:
          ssm_instance_id={{ ssm_instance_id | default('NOT SET') }}
          aws_region={{ aws_region | default('NOT SET') }}
          ssm_bucket_name={{ ssm_bucket_name | default('NOT SET') }}

    - name: Check Python and pip availability
      ansible.builtin.shell: |
        echo "Python path: $(which python3)"
        echo "Python version: $(python3 --version)"
        echo "Pip check: $(python3 -m pip --version 2>&1 || echo 'pip NOT FOUND')"
        echo "Pip3 check: $(pip3 --version 2>&1 || echo 'pip3 NOT FOUND')"
        echo "Available pythons:"
        ls /usr/bin/python* 2>/dev/null || echo "none in /usr/bin"
        ls /usr/local/bin/python* 2>/dev/null || echo "none in /usr/local/bin"
      register: python_check
      delegate_to: localhost
      become: false

    - name: Show Python environment
      ansible.builtin.debug:
        msg: "{{ python_check.stdout_lines }}"

    - name: Install boto3 and botocore via pip
      ansible.builtin.shell: |
        /usr/bin/python3 -m pip install --upgrade boto3 botocore
      register: pip_install
      changed_when: "'Successfully installed' in pip_install.stdout"
      delegate_to: localhost
      become: false

    - name: Check if AWS CLI is already installed
      ansible.builtin.shell: which aws && aws --version
      register: aws_cli_check
      failed_when: false

    - name: Install AWS CLI if not present
      ansible.builtin.shell: |
        curl -sL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" \
          -o /tmp/awscliv2.zip
        unzip -q /tmp/awscliv2.zip -d /tmp/
        /tmp/aws/install --update
        rm -rf /tmp/aws /tmp/awscliv2.zip
      when: aws_cli_check.rc != 0

    - name: Check if SSM plugin is already installed
      ansible.builtin.shell: which session-manager-plugin && session-manager-plugin --version
      register: ssm_plugin_check
      failed_when: false

    - name: Install SSM Session Manager plugin if not present
      ansible.builtin.shell: |
        curl -sL "https://s3.amazonaws.com/session-manager-downloads/plugin/latest/linux_64bit/session-manager-plugin.rpm" \
          -o /tmp/session-manager-plugin.rpm
        rpm -i /tmp/session-manager-plugin.rpm
        rm -f /tmp/session-manager-plugin.rpm
      when: ssm_plugin_check.rc != 0

    - name: Install community.aws collection
      ansible.builtin.command: >
        ansible-galaxy collection install community.aws --ignore-certs
      register: collection_install
      changed_when: "'Installing' in collection_install.stdout"

    - name: Wait for SSM agent to be online
      ansible.builtin.shell: |
        aws ssm describe-instance-information \
          --filters "Key=InstanceIds,Values={{ hostvars[groups['all'][0]]['ssm_instance_id'] }}" \
          --region {{ hostvars[groups['all'][0]]['aws_region'] }} \
          --query 'InstanceInformationList[0].PingStatus' \
          --output text
      register: ssm_status
      retries: 12
      delay: 15
      until: ssm_status.stdout.strip() == "Online"

    - name: Wait for SSM agent to be online
      ansible.builtin.shell: |
        aws ssm describe-instance-information \
          --filters "Key=InstanceIds,Values={{ ssm_instance_id }}" \
          --region {{ aws_region | default('us-east-1') }} \
          --query 'InstanceInformationList[0].PingStatus' \
          --output text
      register: ssm_status
      retries: 12
      delay: 15
      until: ssm_status.stdout.strip() == "Online"
      when: ssm_instance_id is defined and ssm_instance_id != ''

# -------------------------------------------------------
# Play 2 — Configure EC2 instance via SSM
# All your existing tasks unchanged — just moved here
# -------------------------------------------------------
- name: Configure demo instance — AAP runtime layer
  hosts: all
  become: true
  gather_facts: false
  connection: community.aws.aws_ssm

  vars:
    config_version: "1.3.0"
    aap_job_id: "{{ tower_job_id | default('unknown') }}"
    ansible_aws_ssm_instance_id: "{{ ssm_instance_id | default('') }}"
    ansible_aws_ssm_region: "{{ aws_region | default('us-east-1') }}"
    ansible_aws_ssm_bucket_name: "{{ ssm_bucket_name | default('') }}"
    ansible_python_interpreter: "/usr/bin/python3"

  tasks:

    - name: Wait for cloud-init to complete
      ansible.builtin.command: cloud-init status --wait
      changed_when: false

    - name: Gather facts
      ansible.builtin.setup:
        gather_subset:
          - network
          - hardware
          - virtual

    # -------------------------------------------------------
    # Task 1 — Update the version page with live runtime info
    # -------------------------------------------------------
    - name: Inject runtime details into existing Packer page
      ansible.builtin.blockinfile:
        path: /usr/share/nginx/html/index.html
        marker: "<!-- {mark} ANSIBLE MANAGED BLOCK -->"
        block: |
          <div style="border-top:2px solid #EE0000;margin-top:20px;padding-top:20px;">
          <h2><span class='badge ansible'>Ansible Automation Platform</span></h2>
          <table>
            <tr><th>Config version</th><td>{{ config_version }}</td></tr>
            <tr><th>Hostname</th><td>{{ ansible_hostname }}</td></tr>
            <tr><th>IP address</th><td>{{ ansible_default_ipv4.address }}</td></tr>
            <tr><th>Total memory</th><td>{{ ansible_memtotal_mb }} MB</td></tr>
            <tr><th>CPU cores</th><td>{{ ansible_processor_vcpus }}</td></tr>
            <tr><th>AAP job ID</th><td>{{ aap_job_id }}</td></tr>
            <tr><th>Configured at</th><td>{{ ansible_date_time.iso8601 }}</td></tr>
          </table>
          </div>

    # -------------------------------------------------------
    # Task 2 — Write the /status endpoint
    # -------------------------------------------------------
    - name: Write runtime status page
      ansible.builtin.copy:
        dest: /var/www/status/index.html
        content: |
          <!DOCTYPE html>
          <html>
          <head><title>Instance Status</title>
          <style>body{font-family:monospace;max-width:800px;margin:40px auto;padding:0 20px;}
          .ok{color:green;font-weight:bold;}</style></head>
          <body>
          <h1>Instance Health Status</h1>
          <p>Status: <span class='ok'>HEALTHY</span></p>
          <h2>Ansible Audit Trail</h2>
          <pre>
          Job ID    : {{ aap_job_id }}
          Timestamp : {{ ansible_date_time.iso8601 }}
          Host      : {{ ansible_hostname }}
          IP        : {{ ansible_default_ipv4.address }}
          Version   : {{ config_version }}
          </pre>
          </body></html>
        mode: '0644'

    # -------------------------------------------------------
    # Task 3 — Write audit file
    # -------------------------------------------------------
    - name: Write Ansible audit file
      ansible.builtin.copy:
        dest: /etc/ansible-configured
        content: |
          Configured by Ansible Automation Platform
          ==========================================
          Timestamp : {{ ansible_date_time.iso8601 }}
          AAP job   : {{ aap_job_id }}
          Version   : {{ config_version }}
          Host      : {{ ansible_hostname }}
          IP        : {{ ansible_default_ipv4.address }}
        mode: '0644'

    # -------------------------------------------------------
    # Task 4 — Ensure nginx is serving correctly
    # -------------------------------------------------------
    - name: Ensure nginx is running
      ansible.builtin.systemd:
        name: nginx
        state: started
        enabled: true

    - name: Verify index page is reachable
      ansible.builtin.uri:
        url: "http://{{ ansible_default_ipv4.address }}"
        return_content: true
        status_code: 200
      register: health_check

    - name: Verify status page is reachable
      ansible.builtin.uri:
        url: "http://{{ ansible_default_ipv4.address }}/status"
        return_content: true
        status_code: 200

    - name: Report success
      ansible.builtin.debug:
        msg: >
          Instance {{ ansible_hostname }} configured successfully.
          Version {{ config_version }} is live at http://{{ ansible_default_ipv4.address }}